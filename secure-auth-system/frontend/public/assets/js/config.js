// Deployment configuration — EDIT THIS FILE before deploying.
//
// Local development: leave as-is (points to your local backend on port 3000).
// Production: change API_BASE_URL to your deployed backend's URL, e.g.
//   window.API_BASE_URL = 'https://secure-auth-backend.up.railway.app/api';
//
// This must be loaded BEFORE api.js in every HTML page (already wired up).

window.API_BASE_URL = 'http://localhost:3000/api';
